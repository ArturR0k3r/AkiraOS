# SPDX-License-Identifier: Apache-2.0
#
# Test-scope Kconfig for the AkiraOS ztest suite.
#
# Zephyr's kconfig.cmake auto-discovers this file via APPLICATION_SOURCE_DIR.
# The AkiraOS symbols come from the akira-os module (zephyr/Kconfig) and are
# enabled by CONFIG_AKIRA_OS=y in prj.conf.

source "Kconfig.zephyr"
