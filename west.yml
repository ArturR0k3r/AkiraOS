# west.yml - West workspace manifest
manifest: 
  projects:
    # Zephyr RTOS
    - name: zephyr
      url: https://github.com/PenEngineering/zephyr
      revision: AkiraConsole-fix
      west-commands: scripts/west-commands.yml
      import: true
    
    # AkiraPlatform (optional enterprise features)
    # Add via build.sh --platform flag to include in compilation
    #- name: akira-platform
    #  url: https://github.com/PenEngineering/AkiraPlatform
    #  revision: main
    #  path: akira-platform
      
  self:
    path: akira-os


